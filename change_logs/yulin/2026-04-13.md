# qbb 2026/4/13

## 工作内容：workflow 统一动态化、执行链路收敛与冗余清理

---

### 1. 架构统一（default / 自定义 / plan）

#### `workflow/graph_builder.py`
- 移除硬编码 `build_graph()` 路径，统一改为动态配置构图：
  - `load_workflow_graph_config(workflow_name)`
  - `build_app_from_workflow(workflow_name, ...)`
  - `build_dynamic_graph(nodes, edges, ...)`
- 所有入口（`default`、`task --wf`、`plan`）最终都汇聚到 `build_dynamic_graph()`。
- 当动态节点为空时，回退到 `default workflow` 配置图，而非旧硬编码图。

#### `config/workflow_registry.json`
- `default` 改为 `type=file`，统一走配置驱动。

#### `config/workflow_default_dynamic.json`（新建）
- 新增默认三节点动态配置（design → think → execute），替代旧硬编码默认链路。

#### `config/workflow_five_nodes_example.json`（新建）
- 新增 5 节点自定义 workflow 示例，供 `task --wf` 演示与测试。

#### `workflow/workflow_registry.py`
- 去除 `builtin_default` 分支，统一只处理 `file` 规格（兼容字符串路径简写）。

---

### 2. 执行器合并与调用链收敛

#### `core/agent_cli.py`
- 抽出统一执行器 `_execute_with_app()`，负责：
  - 上下文历史装配
  - `initial_state` 构建
  - `app.invoke` + loading
  - 消息回写 `ContextManager`
- `run_task()` 与 `run_plan_task()` 均复用同一执行器，仅保留各自前置步骤差异。
- 删除不再使用的 `self.app` 运行缓存字段与相关依赖。
- 清理未使用导入（如 `BRANCH_ALIASES`）。

#### `main.py`
- `plan/mas` 分支收敛为 `cli.run_plan_task(task)`，不再在 `main.py` 中手写规划后执行细节。

---

### 3. 日志与调试能力统一

#### `agents/simple_agent.py`
- 新增统一 LLM 交互日志输出：
  - 文件：`logs/llm_interactions_trace.txt`
  - 覆盖范围：默认 / 自定义 / plan（所有走 `SimpleAgent.run()` 的路径）
  - 记录内容：完整消息序列 + 模型响应 + agent/model/temperature 元信息。

#### `workflow/nodes.py`
- 移除旧的“仅默认 task”日志写入逻辑（`default_task_llm_trace.txt` 相关实现）。

#### `logs/default_task_llm_trace.txt`
- 删除旧文件，统一改为 `llm_interactions_trace.txt`。

---

### 4. 无用代码与模块清理

#### 删除文件
- `workflow/edges.py`
- `config/workflow_config.py`

#### `workflow/nodes.py`
- 删除已失效的旧默认链路节点实现：
  - `make_retrieve_node`
  - `make_design_node`
  - `make_think_node`
  - `make_execute_node`
  - `_task_adaptive_instruction`
- 当前保留并使用的节点工厂为 `make_agent_node()`。

#### `workflow/__init__.py`
- 导出接口更新为动态构图相关函数（与当前架构一致）。

---

### 5. 问题修复记录

| 问题 | 原因 | 修复 |
|---|---|---|
| `NameError: WorkflowRegistry is not defined` | `agent_cli` 迁移后残留旧字段初始化 | 删除残留引用 |
| `NameError: Tuple is not defined` | `graph_builder` 类型注解保留但导入遗漏 | 补回 `Tuple` 导入 |
| plan/task 记忆行为不一致 | plan 分支历史执行路径未完全收敛 | 统一进入 `TeXAgentCLI` 执行器 |

---

### 6. 当前结果

- 三类模式（默认 / 自定义 / plan）底层构图已统一为动态 workflow。
- 三类模式执行器已统一，状态装配与上下文回写逻辑一致。
- 旧硬编码构图与线性边模块已移除，避免后续维护双轨。

---

### 7. 当日追加修复（上下文记忆实效）

#### `workflow/nodes.py`
- 修复 `make_agent_node()` 未注入历史上下文的问题：
  - 新增 `ctx.build(state, memory=..., config=...)` 调用
  - 在 prompt 中加入 `[历史上下文]` 段，确保跨轮任务可继承上下文
  - 默认窗口参数：`conv_limit=12`、`mem_limit=5`、`max_tokens=8000`（支持在 node_config 覆盖）

#### `core/agent_cli.py`
- 修复上下文回写重复问题：
  - 由“全量写回 result['messages']”改为“仅回写本轮新增消息”
  - 通过 `len(history_messages)` 计算切片，避免历史消息重复积累导致污染

#### 修复效果
- `plan` 后接 `task` 的追问可承接前文语境，不再出现“已有上下文却要求补充主题”的退化行为。
