# file_loading_tool.py

## 模块说明

FileLoadingTool：加载指定文件路径的文件内容并返回。

## API 概览

### 类

- `FileLoadingTool`：文件加载工具。

## 类与方法

### FileLoadingTool

文件加载工具。

方法：

- `__init__(self)`：初始化文件加载工具，并声明支持的输入路径参数 schema。
- `run(self, file_path)`：执行文件加载操作。
