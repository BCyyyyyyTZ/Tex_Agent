langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-core>=0.3.0

pydantic>=2.0.0

python-dotenv>=1.0.0

arxiv>=2.1.0

httpx>=0.27.0

# Web 交互 UI
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
anyio>=4.0.0
python-multipart>=0.0.9

google-genai>=1.0.0

# RAG 依赖
# chromadb 内置 DefaultEmbeddingFunction（基于 all-MiniLM-L6-v2 / ONNX Runtime）
# 首次运行会自动下载约 40MB 的 ONNX 模型文件（需联网），此后完全离线运行
chromadb>=0.5.0

pytest>=7.0.0
pytest-asyncio>=0.21.0

# LaTeX 纠错/润色基础
pylatexenc>=2.10

# 目录监视
watchdog>=4.0.0

# 幽灵窗口服务（阶段 10）依赖 FastAPI/uvicorn/httpx（上方已包含），无需额外 pip 包
# 说明：LaTeX 编译诊断还依赖系统命令（非 pip）：
#   1) TeX 发行版（TeX Live / MiKTeX）
#   2) latexmk（编译日志诊断）
#   3) chktex（静态检查）
# 请确认上述命令在 PATH 中可执行，否则将退化为仅部分能力可用。

# PDF 处理依赖
pymupdf>=1.24.0
pypdf>=4.2.0
pdfplumber>=0.11.0
# file_loading_tool 读取 PDF / Word（按需导入；未安装时仍可启动 plan 等不读 PDF 的流程）
PyPDF2>=3.0.0
python-docx>=1.1.0

# 文档解析工具
docling>=2.0
pymupdf>=1.24.0