我和团队想要开发一个基于多智能体架构的 LaTeX 论文写作增强系统。下面是我们的系统核心需求和架构设计。项目名：`TeX_Agent`

你的任务是：作为资深架构师，根据我们提供的需求文档，结合下面我们的一些说明和要求，为我们搭建初始的 Python 项目代码骨架。

为了保证我们 4 个人能够高效并行开发，且项目能稳步迭代，请你严格遵循以下要求：

1. 交互式工作流（分步执行）

第一步，请先输出你建议的项目文件目录树结构，以及一份完整的 `README.md`。在 README 中，除了说明架构思路和各模块功能，并附上项目目录树结构、以及具体每个文件/目录的作用或功能，还请务必增加一个“团队协作分工建议”章节，清晰地将各目录/模块划分给 4 位开发者。

暂停等待：输出完第一步后，请立即停止生成，并在末尾询问我：“目录结构和 README 是否符合预期？如果确认无误，我将开始生成具体的代码骨架。”（在我回复确认前，不要输出任何代码）。

2. 严格的 “[扩展]” 标签管理与代码闭环

无标签部分（最小可运行 MVP）：对于没有标注“[扩展]” 的模块，你需要实现完整且可以直接运行的代码。必须提供一个清晰的程序入口（如 `main.py`），确保我复制粘贴后就能直接 run 起来一个基础链路。

带“[扩展]” 标签部分（接口占位）：对于标注了“[扩展]” 的模块，绝对不要实现业务逻辑。请使用抽象基类（ABC）、接口定义、清晰的 Type Hints 和带有详细 Docstring 的 `pass` 或 `raise NotImplementedError` 进行占位。

运行代码中的扩展锚点：在实现“最小可运行”代码时，也必须在关键节点留下形如 `# TODO: 未来在此处接入动态路由策略` 的注释，告诉我们未来在哪里注入复杂逻辑。

3. 极致解耦（面向 4 人并行开发）

核心思想是“基于接口编程”。所有的 Agent、Tool、Memory 在相互调用时，都必须依赖基类接口而非具体实现。这样我们在分别开发各自模块时，只需要 mock 对方的接口即可运行测试。

4. 重点设计数据流转（State）和基础接口，特别是为“多分支上下文机制”和“动态路由”预留好底层数据结构。

5. 你需要考虑一个成熟完备的agent框架所需要的基本的逻辑（比如并发等等，要求是基本且常见的设计，这些设计也许不会在我的需求中提到）



论文协作写作智能体 (TeX_Agent) 核心架构需求
一、 系统核心模块与接口需求 (请根据此构建骨架)

请充分考虑解耦性，我们希望可以实现并行开发。

1. 多智能体工作流与编排 (LangGraph Workflow)

  基础图结构 (可运行)：实现一个最简单的多智能体链式/图结构。包含一轮最基础的“Design (设计) -> Think (思考) -> Execute (执行)”节点流转。

  节点定义与搭建 (可运行)：实现基本的节点包装器 (Node Wrappers) 并在 LangGraph 中建立有向边，跑通上述简单工作流。

  全局状态 State (可运行)：定义包含消息历史、当前节点状态的基础 `TypedDict` 或 Pydantic Model。

  *[扩展] 复杂循环与动态边*：预留 `add_conditional_edges` 的接口与复杂分支逻辑。

  *[扩展] 用户自定义工作流*：预留支持用户通过配置文件动态组装 Graph 节点的解析接口。

2. 智能体模块 (Agents) 与通信

  通信协议 (可运行)：定义一个最简单的 Agent 间通信协议（例如标准的 Message 字典/对象载体）。

  基础 Agent (可运行)：实现一个简单的 Agent 类，能够接收上游节点的输入，执行基础推理，并返回结果。

  *[扩展] 高阶 Agent 架构*：定义 `ReActAgent`、`ReflectionAgent`、`PlanAndSolveAgent` 的标准抽象基类（BaseAgent）和接口。

  *[扩展] 情绪与陪伴价值*：所有 Agent 预留“情感与情绪分析”的 Hook 接口，用于感知用户情绪。

3. 工具与技能模块 (Tools / Skills)

  BaseTool 接口 (可运行)：定义标准的工具基类。

  基础功能实现 (可运行)：实现1个极其简单的工具并接入框架：1调用 arXiv API 进行基础文献检索。

  *[扩展] 高级 LaTeX 解析*：预留识别语法错误、结构化解析 AST 树的接口。

  *[扩展] 数据与可视化*：预留调用 Matplotlib/Seaborn 的接口。

  *[扩展] 图像生成*：预留 DALL-E / Stable Diffusion 等 API 接入点。

4. 知识增强与记忆模块 (RAG & Memory)

   基础上下文 (可运行)：实现一个能在单次运行周期内存取消息记录的基础 Context 管理器。

   *[扩展] 多分支上下文机制*：这是未来的核心！设计一种类似 Git Branch 的状态管理数据结构接口（如 `BranchNode`, `ContextTree`），预留状态无缝迁移与回退的底层结构。

   *[扩展] 向量知识库*：预留接入多类别 RAG（检索资料库、专家库）的接口。

5. 协同与控制模块 (Controller / Router)

   *[扩展] 自适应路由 (Router)*：预留基于任务复杂度动态分配 Agent 的路由模块接口。

   *[扩展] MAS Planner*：预留主控引擎的任务分解与验证接口。

6. 安全与权限模块 (Security)

   *[扩展]* 预留权限控制与数据保护的拦截器/中间件接口。



三、 期望输出格式要求
1. 请首先输出你建议的项目目录树结构，以 TeX_Agent/ 为根目录。
2. 提供一份清晰的 README.md 内容概览，说明架构设计思路，并附上项目目录树结构、以及具体每个文件/目录的作用或功能（简要介绍即可）；希望附上分工建议
3. 按照目录结构和README的结构设计，结合我上边的提示，生成可运行的代码以及其他内容的接口、框架实现