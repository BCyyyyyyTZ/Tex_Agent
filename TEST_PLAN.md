# TeX\_Agent 测试计划

## 1. 文档信息

- 项目：TeX\_Agent
- 测试范围：Python 后端、工作流引擎、工具体系、RAG 管线、Web UI（后端接口 + 静态资源完整性）、CLI/路由/配置/通用工具等
- 测试框架：pytest，pytest-asyncio

## 2. 测试目标

1. **正确性**：验证各模块的核心输入/输出协议、数据结构、关键流程分支、错误处理逻辑符合预期。
2. **回归保护**：对关键协议（消息格式、工具返回、工作流状态写回）建立稳定用例，避免未来重构破坏兼容性。
3. **可维护性**：测试用例以“单元测试优先、集成测试分层”的方式组织

## 3. 测试范围与对象

本计划覆盖项目根目录下全部主要功能模块：

- `agents/`：Agent 抽象与具体实现、LLM 客户端封装
- `tools/`：工具基类、各工具（arxiv/diagram/chart\_plot/latex\_autofix/pdf/comment/...）
- `workflow_engine/`：工作流消息协议、节点执行、workflow 运行与合并逻辑
- `workflow/`：workflow 配置解析与图构建
- `core/`：消息协议、异常、状态等核心基础层
- `context/`、`memory/`：上下文/记忆数据结构与基本行为
- `rag/`：检索增强管线
- `router/`、`cli/`：路由/命令解析等
- `security/`：中间件/安全策略
- `utils/`：日志、并发工具等
- `ui/web/`：Web UI 后端模块与静态资源
- `scripts/`：辅助脚本做幂等性与输出结构验证

## 4. 不在范围

为保证测试“可重复、离线可跑、不会影响环境”，以下内容不作为强制通过条件：

- 外部服务调用：Google Gemini、arXiv 在线 API、mermaid.ink 在线渲染等（在具备网络与凭据时作为集成测试执行；单元测试覆盖入参校验、输出协议与本地解析/编码逻辑）
- LaTeX 编译（依赖本机 TeX 发行版与环境）：在具备编译器时执行最小工程冒烟验证；在不具备编译器时要求给出明确失败原因
- 浏览器端交互 E2E（JS 运行时、Playwright 等）：仅做静态资源完整性与关键文件存在性检查

## 5. 测试策略与分层

### 5.1 单元测试

目标：在不依赖外部系统的情况下，对函数/类方法进行确定性验证。

- 依赖隔离：尽量使用“最小可运行输入 + 本地临时文件/目录”覆盖确定性逻辑；对可选依赖按存在性分支验证
- 数据驱动：对边界条件与异常分支使用参数化用例
- 失败可定位：每条用例只验证一个核心行为点，并在断言中附带清晰错误信息

### 5.2 集成测试

目标：验证模块之间的组合行为（例如 workflow\_engine 运行、RAG 管线）。

- 标记：使用 `@pytest.mark.integration`
- 跳过策略：
  - 外部依赖缺失：`pytest.importorskip(...)`
  - 环境变量开关：例如遵循项目内已存在的 `SKIP_*` 跳过模式

### 5.3 冒烟测试

目标：快速验证项目关键模块可被 import、关键配置与静态资源存在、主入口可解析。

## 6. 测试设计方法

- **等价类/边界值**：对输入为空、缺字段、非法类型、极端长度等场景覆盖
- **状态迁移**：对 workflow 的状态写回与消息合并做状态变更断言
- **异常路径覆盖**：对常见错误（IOError/ValueError/HTTPException/subprocess error）做断言
- **契约测试**：对 ToolResult/WorkflowMessage 等协议做序列化一致性验证

## 7. 测试环境与工具

- Python：以项目当前运行环境为准
- 测试框架：pytest + pytest-asyncio
- 依赖说明：
  - 可选依赖（docling/chromadb/fitz/matplotlib 等）在测试中按依赖可用性进行分支验证或使用 `importorskip`，保证测试可在不同环境下稳定运行
- 目录约定：
  - 测试代码：`tests/`（pytest.ini 指定收集路径）
  - 测试数据：优先放在 `tests/testdata/` 或复用已有 `tests/test_rag/test_document/`

## 8. 入口/退出准则

### 8.1 入口准则

- 仓库可被 Python import
- 不修改任何生产功能代码

### 8.2 退出准则

- 单元测试：在无外部服务条件下通过
- 集成测试：在满足依赖与环境条件时通过；否则允许按 marker/环境变量跳过

## 9. 覆盖矩阵

下表描述“模块 → 关注点 → 测试类型”的覆盖关系（详细用例见测试代码注释）：

- `core/`：消息兼容、序列化、默认值、错误类型（单元）
- `workflow_engine/`：消息类型、节点执行适配、合并/路由（单元 + 少量集成）
- `tools/`：
  - `command_running`：执行无副作用命令的成功/失败路径与输入校验（单元）
  - `docling_search`：候选解析、文本节点抽取、模糊搜索与 export 输出（单元，tmp\_path）
  - `rag_retrieve`：输入校验、空库兜底、text/json 输出协议（单元，最小可用检索管线实现）
  - `chapter_index`：章节解析、层级/孤立节标注、stats/tree/json 输出（单元，tmp\_path）
  - `markdown_section`：大纲/内容提取、关键词匹配与兜底策略（单元，tmp\_path）
  - `figure_ref_checker`：定义/引用提取、编号跳号检测（单元）
  - `pdf_comment`：文本定位与批注输出（单元，最小 PDF 构造；依赖缺失可跳过）
  - `file_loading`：文本/编码路径覆盖（单元，tmp\_path；PDF/Word 依赖缺失可跳过）
  - `chart_plot`：入参校验与图像文件输出（单元；matplotlib 不可用时验证依赖错误）
  - `concept_diagram`：入参校验、Mermaid 提取与编码（单元；外部服务作为集成测试项）
  - `latex_autofix`：错误解析、规则修复、多 edits 应用、最小工程运行路径（单元/冒烟；编译器可用时应成功）
- `agents/`：LLM 客户端接口约束、关键入参处理（单元）
- `ui/web/`：关键函数（例如回复格式化、DAG 校验）与静态资源存在性（单元/冒烟）
- `rag/`：复用现有测试；新增补充用例以覆盖边界与异常（单元 + 可选集成）

## 10. 风险与缓解

- 外部依赖/网络波动：以集成测试分层 + 可跳过策略控制；单元测试优先覆盖本地可确定行为
- 非确定性输出（LLM）：只测试“提示词构造/解析/应用逻辑”，不测试模型语义质量
- OS 差异（Windows 路径）：测试使用 `pathlib.Path` 与 `tmp_path`，并避免硬编码分隔符

## 11. 维护策略

- 新增模块：必须新增对应单元测试文件，并纳入覆盖矩阵
- 修复缺陷：先写回归测试再修复

