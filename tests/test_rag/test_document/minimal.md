# 测试文档这是一段用于 RAG 分块测试的短 Markdown。第二句用来增加长度。

## 小节 A

- 列表项一
- 列表项二

## 小节 B

结论：单元测试应使用可控的小文件，而非完整论文 PDF。


# Related Work（相关工作）

Transformer 架构由 Vaswani et al. 在 *Attention Is All You Need* 中提出。Self-attention 允许序列内任意位置直接交互。

BERT 使用 masked language modeling；GPT 系列采用 causal LM。二者均基于 Transformer encoder或 decoder 堆叠。

关键词：RAG, retrieval-augmented generation,向量检索, ChromaDB.